/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.id, this.name, this.count, this.time);

  final String name;
  final String time;
  final int id;
  final int count;
}
